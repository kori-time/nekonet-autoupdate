# Failover

The service models A/B coordination and exposes coordinator state. Continuous heartbeat takeover and exact checkpoint resume are part of the v0.1 acceptance tests but must be validated in a two-node staging deployment.
